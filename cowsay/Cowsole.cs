using System;
using cowsaу;

namespace cowsay
{
    internal static class Cowsole
    {
        internal static void WriteLine(string message) => Console.WriteLine(message);
        internal static void Write(string message) => Console.Write(message);
    }
}
